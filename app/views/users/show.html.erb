<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1><%= @user.name %></h1>
      <p>Member since <%= @user.created_at %></p>
    </section>
  </aside>
  <div class="col-md-8">
    <h3>Hosted events</h3>
  </div>
</div>
